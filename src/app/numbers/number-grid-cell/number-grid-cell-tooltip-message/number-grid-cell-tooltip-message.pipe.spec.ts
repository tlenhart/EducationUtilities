import { NumberGridCellTooltipMessagePipe } from './number-grid-cell-tooltip-message.pipe';

describe('NumberGridCellTooltipMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberGridCellTooltipMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
