import { DayOfWeekEnumPipe } from './day-of-week-enum.pipe';

describe('DayOfWeekEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new DayOfWeekEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
