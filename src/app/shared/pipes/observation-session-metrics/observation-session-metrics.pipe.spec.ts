import { ObservationSessionMetricsPipe } from './observation-session-metrics.pipe';

describe('ObservationSessionMetricsPipe', () => {
  it('create an instance', () => {
    const pipe = new ObservationSessionMetricsPipe();
    expect(pipe).toBeTruthy();
  });
});
